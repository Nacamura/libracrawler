<h1>登録済み書籍</h1>
<h2>この一覧は検索でヒットしても無視されます</h2>

<table>
  <tr>
    <th>書名</th>
    <th>著者</th>
    <th>出版社</th>
    <th>出版年</th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.author %></td>
    <td><%= book.publisher %></td>
    <td><%= book.year %></td>
  </tr>
<% end %>
</table>

<%= link_to '戻る', libraries_path %>

<br /><br />
